{
  "name": "mu-workspace",
  "version": "1.0.0",
  "private": true,
  "packageManager": "yarn@1.22.22",
  "workspaces": [
    "mu-ui-lib",
    "mmemo/front",
    "home-sign/front",
    "sail/front"
  ],
  "engines": {
    "node": ">=24.13.1 <25",
    "yarn": "1.22.22"
  },
  "scripts": {
    "dev:ui": "yarn workspace mu-ui-lib dev",
    "build:ui": "yarn workspace mu-ui-lib build",
    "check:ui": "yarn workspace mu-ui-lib run check",
    "dev:mmemo": "yarn workspace mmemo dev",
    "dev:home-sign": "yarn workspace home-sign dev",
    "dev:sail": "yarn workspace sail dev",
    "dev:all": "concurrently \"yarn dev:ui\" \"yarn dev:mmemo\" \"yarn dev:home-sign\" \"yarn dev:sail\"",
    "build:mmemo": "yarn workspace mmemo build",
    "build:home-sign": "yarn workspace home-sign build",
    "build:sail": "yarn workspace sail build",
    "build:all": "yarn build:ui && yarn build:mmemo && yarn build:home-sign && yarn build:sail",
    "check:mmemo": "yarn workspace mmemo run check",
    "check:home-sign": "yarn workspace home-sign run check",
    "check:sail": "yarn workspace sail run check",
    "check:all": "yarn check:ui && yarn check:mmemo && yarn check:home-sign && yarn check:sail",
    "install:all": "yarn install --ignore-scripts"
  },
  "devDependencies": {
    "concurrently": "^9.0.0"
  }
}
